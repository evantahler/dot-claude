# Claude Code ephemeral data
backups/
cache/
debug/
file-history/
ide/
paste-cache/
plans/
policy-limits.json
remote-settings.json
session-env/
sessions/
shell-snapshots/
stats-cache.json
statsig/
tasks/
telemetry/
todos/
usage-data/
*.jsonl

# Project-specific data (per-project CLAUDE.md and memory live in each repo)
projects/

# Plugins (fetched/installed locally)
plugins/

# Dependencies (bun install)
node_modules

# Ephemeral sync tracking
last-synced-at

# Local settings (machine-specific permissions, not shared)
settings.local.json
.claude/settings.local.json

# OS
.DS_Store
